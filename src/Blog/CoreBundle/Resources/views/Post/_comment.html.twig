<article class="comment">
    <header>
        <p>
            {{- 'blog.core.post.comment.on' | trans }} <time datetime=" {{ comment.createdAt | date('c') }}">{{ comment.createdAt | date }}</time>
            |
            {{ 'blog.core.post.comment.by' | trans }}: {{ comment.authorName | title -}}
        </p>
    </header>
    <p>{{ comment.body | nl2br }}</p>
</article>
